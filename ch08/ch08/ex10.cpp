#include <stdio.h>

int main() {
    int a = 106, b = 83;

    printf("a & b = %d\n", a & b);  // 비트 AND
    printf("a | b = %d\n", a | b);  // 비트 OR
    printf("a ^ b = %d\n", a ^ b);  // 비트 XOR
    printf("~a    = %d\n", ~a);     // 비트 NOT
}
